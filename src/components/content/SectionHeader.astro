---
interface Props {
  command: string;
  description: string;
}

const { command, description } = Astro.props;
---

<div class="flex flex-col gap-2">
  <h2 class="text-[28px] font-semibold text-green-primary font-mono">{command}</h2>
  <p class="text-[13px] text-text-tertiary font-mono">{description}</p>
</div>
